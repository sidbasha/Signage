# 5. API reference

> Endpoint tables are generated by `docs/_tools/gen_api_doc.py` from the controllers and `docs/api/openapi.json`; examples are real responses captured from a running instance (`docs/api/samples.json`). The interactive version is at **`/swagger`** (enabled in Development, or with `Swagger__Enabled=true`).

## 5.1 Conventions

| Topic | Rule |
|---|---|
| Base URL | Same origin as the UI, e.g. `https://signage.example.com`. All endpoints are under `/api`, hubs under `/hubs`. |
| Format | JSON, camelCase. Enums are strings (`"AndroidTv"`). Dates are ISO-8601 UTC; `DateOnly` is `"2026-09-21"`, `TimeOnly` is `"07:00:00"`. |
| IDs | UUIDs. Routes constrain them (`{id:guid}`), so malformed ids give 404. |
| Tenancy | Implicit: the caller's organization comes from the token. Another organization's ids return **404**, never 403. |
| Updates | `PUT` replaces the resource, including child collections (playlist items, layout zones, schedule targets, group members). |
| Pagination | Only `/api/audit-logs` is paged (`page`, `pageSize` → `{ items, total, page, pageSize }`). Other lists return all rows for the organization. |

## 5.2 Authentication, errors and limits

**Admin (JWT).** `POST /api/auth/login` returns a 15-minute access token and a 14-day refresh token. Send `Authorization: Bearer <accessToken>`. On 401, call `POST /api/auth/refresh` once, then retry. The SPA does this with single-flight refresh, because refresh tokens are single use.

```bash
TOKEN=$(curl -s -X POST localhost:5080/api/auth/login -H 'Content-Type: application/json' \
  -d '{"email":"admin@demo.local","password":"Admin@12345"}' | jq -r .accessToken)
curl -s localhost:5080/api/devices -H "Authorization: Bearer $TOKEN" | jq '.[].name'
```

JWT claims: `sub` (user id), `email`, `name`, `jti`, `org`, `org_name`, one `role` per role, and one `perm` per permission. Permission changes take effect when the token next refreshes (≤ 15 min).

**Device (key).** Players send `Authorization: Device <deviceKey>`. On `/hubs/device`, the key may also come as `Bearer` or `?access_token=`. The two schemes are exclusive: a JWT can't call the player API, and a device key can't call admin endpoints.

**Permissions** (23): `dashboard.view`, `organization.manage`, `users.view`, `users.manage`, `roles.manage`, `locations.view`, `locations.manage`, `devices.view`, `devices.manage`, `devices.pair`, `devices.command`, `groups.manage`, `media.view`, `media.manage`, `playlists.view`, `playlists.manage`, `layouts.view`, `layouts.manage`, `schedules.view`, `schedules.manage`, `audit.view`, `subscription.view`, `subscription.manage`.

| Built-in role | Permissions |
|---|---|
| Owner | all 23 |
| Admin | all except `subscription.manage` |
| Editor | every `*.view` except `audit.view`, plus `media.manage`, `playlists.manage`, `layouts.manage`, `schedules.manage`, `devices.command` |
| Viewer | every `*.view` except `audit.view` |

**Errors** use RFC 7807 `application/problem+json`, always with `traceId`. Validation errors add `errors: { field: [messages] }`:

```json
{
  "title": "Validation failed",
  "status": 400,
  "detail": "One or more fields are invalid.",
  "instance": "/api/locations",
  "errors": {
    "name": [
      "name is required."
    ],
    "timeZone": [
      "Choose a valid IANA time zone, for example Asia/Kolkata."
    ]
  },
  "traceId": "0HNOO14QK6A14:00000001"
}
```
```json
{
  "title": "Not found",
  "status": 404,
  "detail": "Device '00000000-0000-0000-0000-000000000001' was not found.",
  "instance": "/api/devices/00000000-0000-0000-0000-000000000001",
  "traceId": "0HNOO14QK6A15:00000001"
}
```

| Status | Meaning |
|---|---|
| 400 | Validation failed (`errors` holds per-field messages; zone errors use keys like `zones[0]`) |
| 401 | Missing, invalid or expired token or device key; wrong password |
| 402 | Plan limit reached (devices, users, storage) or a plan downgrade is blocked by usage |
| 403 | Signed in but missing the permission; read-only resource (template, built-in role); invalid file signature |
| 404 | Not found, or belongs to another organization |
| 409 | Conflict: duplicate email or name, resource still in use |
| 413 | Upload larger than 1 GB |
| 429 | Rate limited: `auth` policy 20 requests/min/IP (all `/api/auth/*`), `pairing` policy 60/min/IP |
| 500 | Unexpected; details only in Development. Search logs by `traceId`. |

### Endpoint index
Every route in the codebase. The "Auth" column is exactly what the controller enforces.

## 5.3 Authentication

| Method | Path | Auth / permission | Request body | Response |
|---|---|---|---|---|
| `POST` | `/api/auth/register` | anonymous · rate limit `auth` | `RegisterRequest` | `AuthResponse` |
| `POST` | `/api/auth/login` | anonymous · rate limit `auth` | `LoginRequest` | `AuthResponse` |
| `POST` | `/api/auth/refresh` | anonymous · rate limit `auth` | `RefreshRequest` | `AuthResponse` |
| `POST` | `/api/auth/logout` | anonymous · rate limit `auth` | `RefreshRequest` | `204` |
| `GET` | `/api/auth/me` | any signed-in user · rate limit `auth` | — | `MeDto` |

**`POST /api/auth/login`**. 

<details><summary>Response 200</summary>

```json
{
  "accessToken": "eyJhbGciOiJIUzI1NiIs\u2026<redacted>",
  "accessTokenExpiresAt": "2026-09-21T18:08:14.7980703Z",
  "refreshToken": "Emuysg\u2026<redacted>",
  "refreshTokenExpiresAt": "2026-10-05T17:53:14.8159004Z",
  "user": {
    "id": "e3886383-d8ef-428a-a067-1e799adfd56c",
    "email": "admin@demo.local",
    "fullName": "Demo Owner",
    "organizationId": "2154068e-318a-4c9d-94aa-8453a9a15798",
    "organizationName": "Demo Signage Co",
    "roles": [
      "Owner"
    ],
    "permissions": [
      "audit.view",
      "dashboard.view",
      "devices.command",
      "devices.manage",
      "\u2026"
    ]
  }
}
```
</details>


**`POST /api/auth/refresh`**. Rotates the refresh token. Reusing a rotated token returns 401 **and revokes every active session of the user**.

**`POST /api/auth/logout`**. Revokes the given refresh token. Always 204.

<details><summary><code>LoginRequest</code> fields</summary>

| Field | Type |
|---|---|
| `email` | string · nullable |
| `password` | string · nullable |
</details>

<details><summary><code>RefreshRequest</code> fields</summary>

| Field | Type |
|---|---|
| `refreshToken` | string · nullable |
</details>

<details><summary><code>RegisterRequest</code> fields</summary>

| Field | Type |
|---|---|
| `organizationName` | string · nullable |
| `fullName` | string · nullable |
| `email` | string · nullable |
| `password` | string · nullable |
| `timeZone` | string · nullable |
</details>

## 5.4 Users and roles

| Method | Path | Auth / permission | Request body | Response |
|---|---|---|---|---|
| `GET` | `/api/users` | `users.view` | — | `UserDto[]` |
| `GET` | `/api/users/{id:guid}` | `users.view` | — | `UserDto` |
| `POST` | `/api/users` | `users.manage` | `CreateUserRequest` | `UserDto` |
| `PUT` | `/api/users/{id:guid}` | `users.manage` | `UpdateUserRequest` | `UserDto` |
| `DELETE` | `/api/users/{id:guid}` | `users.manage` | — | `204` |
| `GET` | `/api/roles` | `users.view` | — | `RoleDto[]` |
| `GET` | `/api/permissions` | `users.view` | — | `PermissionDto[]` |
| `POST` | `/api/roles` | `roles.manage` | `SaveRoleRequest` | `RoleDto` |
| `PUT` | `/api/roles/{id:guid}` | `roles.manage` | `SaveRoleRequest` | `RoleDto` |
| `DELETE` | `/api/roles/{id:guid}` | `roles.manage` | — | `204` |

**`PUT /api/users/{id:guid}`**. Deactivating a user or setting `newPassword` revokes all their refresh tokens.

**`DELETE /api/users/{id:guid}`**. Can't delete yourself; the organization must keep at least one active Owner.

**`PUT /api/roles/{id:guid}`**. Built-in roles (`isSystem`) return 403.

**`DELETE /api/roles/{id:guid}`**. 409 while any user still has the role.

<details><summary><code>CreateUserRequest</code> fields</summary>

| Field | Type |
|---|---|
| `email` | string · nullable |
| `fullName` | string · nullable |
| `password` | string · nullable |
| `roleIds` | string[] · nullable |
</details>

<details><summary><code>SaveRoleRequest</code> fields</summary>

| Field | Type |
|---|---|
| `name` | string · nullable |
| `description` | string · nullable |
| `permissions` | string[] · nullable |
</details>

<details><summary><code>UpdateUserRequest</code> fields</summary>

| Field | Type |
|---|---|
| `fullName` | string · nullable |
| `isActive` | boolean |
| `roleIds` | string[] · nullable |
| `newPassword` | string · nullable |
</details>

## 5.5 Organization, subscription and dashboard

| Method | Path | Auth / permission | Request body | Response |
|---|---|---|---|---|
| `GET` | `/api/organization` | any signed-in user | — | `OrganizationDto` |
| `PUT` | `/api/organization` | `organization.manage` | `UpdateOrganizationRequest` | `OrganizationDto` |
| `GET` | `/api/subscription` | `subscription.view` | — | `SubscriptionDto` |
| `PUT` | `/api/subscription` | `subscription.manage` | `ChangePlanRequest` | `SubscriptionDto` |
| `GET` | `/api/dashboard` | `dashboard.view` | — | `DashboardDto` |
| `GET` | `/api/timezones` | any signed-in user | — | `string[]` |

**`GET /api/subscription`**. 

<details><summary>Response 200</summary>

```json
{
  "plan": "Pro",
  "status": "Active",
  "maxDevices": 100,
  "maxUsers": 25,
  "maxStorageBytes": 107374182400,
  "startsAt": "2026-09-21T15:56:36.933464Z",
  "endsAt": null,
  "usage": {
    "devices": 6,
    "users": 8,
    "storageBytes": 23822
  },
  "availablePlans": [
    {
      "plan": "Free",
      "maxDevices": 3,
      "maxUsers": 3,
      "maxStorageBytes": 2147483648,
      "monthlyPricePerScreen": 0
    },
    {
      "plan": "Pro",
      "maxDevices": 100,
      "maxUsers": 25,
      "maxStorageBytes": 107374182400,
      "monthlyPricePerScreen": 12
    },
    {
      "plan": "Enterprise",
      "maxDevices": 10000,
      "maxUsers": 1000,
      "maxStorageBytes": 2147483648000,
      "monthlyPricePerScreen": 20
    }
  ]
}
```
</details>


**`PUT /api/subscription`**. 402 if current usage exceeds the target plan. No payment is collected (see Security §11.9).

**`GET /api/dashboard`**. 

<details><summary>Response 200 (trimmed)</summary>

```json
{
  "devicesTotal": 6,
  "devicesOnline": 1,
  "devicesOffline": 5,
  "mediaCount": 29,
  "storageBytes": 23822,
  "playlistCount": 17,
  "activeSchedules": 8,
  "unreadNotifications": 4,
  "recentActivity": [
    {
      "id": "7ab65b78-f788-4432-9361-8895dfaba5c6",
      "createdAt": "2026-09-21T17:53:15.685953Z",
      "userEmail": "admin@demo.local",
      "action": "schedule.created",
      "summary": "Created schedule Breakfast menu targeting 1 device(s)/group(s)"
    }
  ],
  "devices": [
    {
      "id": "3ed795ac-c0b6-4828-812b-21008b7fc179",
      "name": "Browser TV 026742",
      "type": "WebPlayer",
      "status": "Offline",
      "orientation": "Landscape",
      "lastSeenAt": "2026-09-21T17:30:26.592805Z",
      "currentItem": null,
      "location": null
    }
  ]
}
```
</details>


**`GET /api/timezones`**. IANA ids (`Region/City` plus `UTC`) from the server OS.

<details><summary><code>ChangePlanRequest</code> fields</summary>

| Field | Type |
|---|---|
| `plan` | string · nullable |
</details>

<details><summary><code>UpdateOrganizationRequest</code> fields</summary>

| Field | Type |
|---|---|
| `name` | string · nullable |
| `defaultTimeZone` | string · nullable |
</details>

## 5.6 Locations

| Method | Path | Auth / permission | Request body | Response |
|---|---|---|---|---|
| `GET` | `/api/locations` | `locations.view` | — | `LocationDto[]` |
| `POST` | `/api/locations` | `locations.manage` | `SaveLocationRequest` | `LocationDto` |
| `PUT` | `/api/locations/{id:guid}` | `locations.manage` | `SaveLocationRequest` | `LocationDto` |
| `DELETE` | `/api/locations/{id:guid}` | `locations.manage` | — | `204` |

<details><summary><code>SaveLocationRequest</code> fields</summary>

| Field | Type |
|---|---|
| `name` | string · nullable |
| `address` | string · nullable |
| `city` | string · nullable |
| `country` | string · nullable |
| `timeZone` | string · nullable |
</details>

## 5.7 Devices and pairing (admin side)

| Method | Path | Auth / permission | Request body | Response |
|---|---|---|---|---|
| `GET` | `/api/devices` | `devices.view` | — | `DeviceDto[]` |
| `GET` | `/api/devices/{id:guid}` | `devices.view` | — | `DeviceDto` |
| `GET` | `/api/devices/{id:guid}/playback` | `devices.view` | — | `200` |
| `POST` | `/api/devices/pair` | `devices.pair` | `PairDeviceRequest` | `DeviceDto` |
| `PUT` | `/api/devices/{id:guid}` | `devices.manage` | `UpdateDeviceRequest` | `DeviceDto` |
| `DELETE` | `/api/devices/{id:guid}` | `devices.manage` | — | `204` |
| `POST` | `/api/devices/{id:guid}/commands` | `devices.command` | `DeviceCommandRequest` | `202` |

**`GET /api/devices`**. Query `locationId`, `groupId`, `status` (Online/Offline).

**`GET /api/devices/{id:guid}/playback`**. Query `days` (1–90, default 7). Plays and seconds per media item.

**`POST /api/devices/pair`**. 400 if the code is unknown or expired, 402 at the plan's device limit.

<details><summary>Response 200</summary>

```json
{
  "id": "8d62f10f-c8cd-4682-a4b6-4721cbc19b2d",
  "name": "Lobby TV",
  "type": "AndroidTv",
  "status": "Offline",
  "orientation": "Landscape",
  "locationId": null,
  "locationName": null,
  "defaultPlaylistId": "3af4ab00-1776-4872-8a27-ff38650a43d4",
  "defaultPlaylistName": "Browser loop 026742",
  "hardwareId": "android-9f2c",
  "resolution": "3840x2160",
  "appVersion": "android-1.0.0",
  "osVersion": "Sony BRAVIA \u00b7 Android 12",
  "ipAddress": "127.0.0.1",
  "pairedAt": "2026-09-21T17:53:15.346379Z",
  "lastSeenAt": null,
  "lastSyncAt": null,
  "syncedVersion": null,
  "currentItem": null,
  "freeStorageBytes": null,
  "groups": []
}
```
</details>


**`DELETE /api/devices/{id:guid}`**. Revokes the key immediately and pushes `Revoked` to the screen.

**`POST /api/devices/{id:guid}/commands`**. Body `{ "command": "identify" | "refresh" | "reload" | "clear-cache" }` → 202. Delivered only if the screen is connected.

<details><summary><code>DeviceCommandRequest</code> fields</summary>

| Field | Type |
|---|---|
| `command` | string · nullable |
</details>

<details><summary><code>PairDeviceRequest</code> fields</summary>

| Field | Type |
|---|---|
| `code` | string · nullable |
| `name` | string · nullable |
| `locationId` | string (uuid) · nullable |
| `defaultPlaylistId` | string (uuid) · nullable |
| `groupIds` | string[] · nullable |
| `orientation` | string · nullable |
</details>

<details><summary><code>UpdateDeviceRequest</code> fields</summary>

| Field | Type |
|---|---|
| `name` | string · nullable |
| `locationId` | string (uuid) · nullable |
| `defaultPlaylistId` | string (uuid) · nullable |
| `groupIds` | string[] · nullable |
| `orientation` | string · nullable |
</details>

## 5.8 Device groups

| Method | Path | Auth / permission | Request body | Response |
|---|---|---|---|---|
| `GET` | `/api/device-groups` | `devices.view` | — | `DeviceGroupDto[]` |
| `GET` | `/api/device-groups/{id:guid}` | `devices.view` | — | `DeviceGroupDto` |
| `POST` | `/api/device-groups` | `groups.manage` | `SaveDeviceGroupRequest` | `DeviceGroupDto` |
| `PUT` | `/api/device-groups/{id:guid}` | `groups.manage` | `SaveDeviceGroupRequest` | `DeviceGroupDto` |
| `DELETE` | `/api/device-groups/{id:guid}` | `groups.manage` | — | `204` |

<details><summary><code>SaveDeviceGroupRequest</code> fields</summary>

| Field | Type |
|---|---|
| `name` | string · nullable |
| `description` | string · nullable |
| `defaultPlaylistId` | string (uuid) · nullable |
| `deviceIds` | string[] · nullable |
</details>

## 5.9 Media and files

| Method | Path | Auth / permission | Request body | Response |
|---|---|---|---|---|
| `GET` | `/api/media` | `media.view` | — | `MediaDto[]` |
| `GET` | `/api/media/{id:guid}` | `media.view` | — | `MediaDto` |
| `POST` | `/api/media` | `media.manage` | `multipart` | `MediaDto` |
| `POST` | `/api/media/web` | `media.manage` | `CreateWebMediaRequest` | `MediaDto` |
| `PUT` | `/api/media/{id:guid}` | `media.manage` | `UpdateMediaRequest` | `MediaDto` |
| `DELETE` | `/api/media/{id:guid}` | `media.manage` | — | `204` |
| `GET` | `/api/files/{id:guid}` | anonymous | — | `file bytes · 206 · 403` |

**`GET /api/media`**. Query `search` (name or tags), `type` (Image/Video/Web).

<details><summary>Response 200</summary>

```json
[
  {
    "id": "922fe173-6eab-4a0d-9503-7ad5f32aaf38",
    "name": "brand-teal",
    "type": "Image",
    "mimeType": "image/png",
    "sizeBytes": 4318,
    "sha256": "06d1dd9a36abd525d2f99959ea0b645336565cfe9da096030646114ff938b62c",
    "width": 1280,
    "height": 720,
    "durationSeconds": 10,
    "url": null,
    "contentUrl": "/api/files/922fe1736eab4a0d95037ad5f32aaf38?exp=1790035200&sig=<hmac>",
    "tags": null,
    "createdAt": "2026-09-21T17:47:12.344918Z",
    "usedInPlaylists": 1
  }
]
```
</details>


**`POST /api/media`**. `multipart/form-data`: `file` (required), `name`, `width`, `height`, `durationSeconds`, `tags`. Max 1 GB. Types: jpg, jpeg, png, gif, webp, mp4, webm, m4v. 402 when over the storage quota.

**`DELETE /api/media/{id:guid}`**. 409 if any playlist uses it (names the playlists).

**`GET /api/files/{id:guid}`**. **Anonymous**: the HMAC signature is the authorization. Query `exp` (unix seconds) and `sig`. Supports `Range` (206). 403 if expired or tampered. Signed URLs come from media and manifest responses; never build them yourself.

<details><summary><code>CreateWebMediaRequest</code> fields</summary>

| Field | Type |
|---|---|
| `name` | string · nullable |
| `url` | string · nullable |
| `durationSeconds` | integer (int32) |
| `tags` | string · nullable |
</details>

<details><summary><code>UpdateMediaRequest</code> fields</summary>

| Field | Type |
|---|---|
| `name` | string · nullable |
| `durationSeconds` | integer (int32) |
| `tags` | string · nullable |
| `url` | string · nullable |
</details>

## 5.10 Playlists and layouts

| Method | Path | Auth / permission | Request body | Response |
|---|---|---|---|---|
| `GET` | `/api/playlists` | `playlists.view` | — | `PlaylistDto[]` |
| `GET` | `/api/playlists/{id:guid}` | `playlists.view` | — | `PlaylistDto` |
| `POST` | `/api/playlists` | `playlists.manage` | `SavePlaylistRequest` | `PlaylistDto` |
| `PUT` | `/api/playlists/{id:guid}` | `playlists.manage` | `SavePlaylistRequest` | `PlaylistDto` |
| `DELETE` | `/api/playlists/{id:guid}` | `playlists.manage` | — | `204` |
| `GET` | `/api/layouts` | `layouts.view` | — | `LayoutDto[]` |
| `GET` | `/api/layouts/templates` | `layouts.view` | — | `LayoutDto[]` |
| `GET` | `/api/layouts/{id:guid}` | `layouts.view` | — | `LayoutDto` |
| `POST` | `/api/layouts` | `layouts.manage` | `SaveLayoutRequest` | `LayoutDto` |
| `POST` | `/api/layouts/from-template/{templateId:guid}` | `layouts.manage` | `FromTemplateRequest` | `LayoutDto` |
| `PUT` | `/api/layouts/{id:guid}` | `layouts.manage` | `SaveLayoutRequest` | `LayoutDto` |
| `DELETE` | `/api/layouts/{id:guid}` | `layouts.manage` | — | `204` |

**`GET /api/playlists/{id:guid}`**. 

<details><summary>Response 200</summary>

```json
{
  "id": "3af4ab00-1776-4872-8a27-ff38650a43d4",
  "name": "Browser loop 026742",
  "description": "",
  "shuffle": false,
  "itemCount": 1,
  "totalDurationSeconds": 10,
  "updatedAt": "2026-09-21T17:30:23.203931Z",
  "items": [
    {
      "id": "1a505b5c-acda-462f-96bc-40074f0762b6",
      "mediaAssetId": "365671ed-d308-49b6-bec5-a8579307b9b1",
      "mediaName": "brand-teal",
      "mediaType": "Image",
      "sortOrder": 0,
      "durationSeconds": null,
      "effectiveDurationSeconds": 10,
      "transition": "fade",
      "contentUrl": "/api/files/365671edd30849b6bec5a8579307b9b1?exp=1790035200&sig=<hmac>"
    }
  ]
}
```
</details>


**`PUT /api/playlists/{id:guid}`**. Replaces all items. Triggers `ContentChanged` to the organization's screens.

**`DELETE /api/playlists/{id:guid}`**. 409 while any schedule uses it; device, group and zone defaults become null.

**`PUT /api/layouts/{id:guid}`**. Templates return 403. Zones must fit 0–100 %. Triggers `ContentChanged`.

**`DELETE /api/layouts/{id:guid}`**. 409 while any schedule uses it; templates return 403.

<details><summary><code>FromTemplateRequest</code> fields</summary>

| Field | Type |
|---|---|
| `name` | string · nullable |
</details>

<details><summary><code>SaveLayoutRequest</code> fields</summary>

| Field | Type |
|---|---|
| `name` | string · nullable |
| `description` | string · nullable |
| `orientation` | string · nullable |
| `width` | integer (int32) |
| `height` | integer (int32) |
| `backgroundColor` | string · nullable |
| `zones` | SaveZone[] · nullable |
</details>

<details><summary><code>SavePlaylistRequest</code> fields</summary>

| Field | Type |
|---|---|
| `name` | string · nullable |
| `description` | string · nullable |
| `shuffle` | boolean |
| `items` | SavePlaylistItem[] · nullable |
</details>

## 5.11 Schedules

| Method | Path | Auth / permission | Request body | Response |
|---|---|---|---|---|
| `GET` | `/api/schedules` | `schedules.view` | — | `ScheduleDto[]` |
| `GET` | `/api/schedules/{id:guid}` | `schedules.view` | — | `ScheduleDto` |
| `POST` | `/api/schedules` | `schedules.manage` | `SaveScheduleRequest` | `ScheduleDto` |
| `PUT` | `/api/schedules/{id:guid}` | `schedules.manage` | `SaveScheduleRequest` | `ScheduleDto` |
| `DELETE` | `/api/schedules/{id:guid}` | `schedules.manage` | — | `204` |

**`POST /api/schedules`**. 

<details><summary>Response 200</summary>

```json
{
  "id": "00f6560b-5c82-4c45-8f7a-ec81ae90e92d",
  "name": "Breakfast menu",
  "layoutId": null,
  "layoutName": null,
  "playlistId": "3af4ab00-1776-4872-8a27-ff38650a43d4",
  "playlistName": "Browser loop 026742",
  "priority": 20,
  "startDate": "2026-09-21",
  "endDate": null,
  "startTime": "07:00:00",
  "endTime": "11:00:00",
  "daysOfWeek": 62,
  "isActive": true,
  "targets": [
    {
      "deviceId": "8d62f10f-c8cd-4682-a4b6-4721cbc19b2d",
      "deviceGroupId": null,
      "name": "Lobby TV"
    }
  ],
  "updatedAt": "2026-09-21T17:53:15.685953Z"
}
```
</details>


<details><summary><code>SaveScheduleRequest</code> fields</summary>

| Field | Type |
|---|---|
| `name` | string · nullable |
| `layoutId` | string (uuid) · nullable |
| `playlistId` | string (uuid) · nullable |
| `priority` | integer (int32) |
| `startDate` | string (date) |
| `endDate` | string (date) · nullable |
| `startTime` | string (time) · nullable |
| `endTime` | string (time) · nullable |
| `daysOfWeek` | integer (int32) |
| `isActive` | boolean |
| `deviceIds` | string[] · nullable |
| `deviceGroupIds` | string[] · nullable |
</details>

## 5.12 Player (device API)

| Method | Path | Auth / permission | Request body | Response |
|---|---|---|---|---|
| `POST` | `/api/pairing` | anonymous · rate limit `pairing` | `CreatePairingRequest` | `PairingCreatedDto` |
| `GET` | `/api/pairing/{id:guid}` | anonymous · rate limit `pairing` | — | `PairingStatusDto` |
| `GET` | `/api/player/manifest` | Device key | — | `DeviceManifest · 304` |
| `POST` | `/api/player/heartbeat` | Device key | `HeartbeatRequest` | `204` |
| `POST` | `/api/player/playback` | Device key | `PlaybackEntry[]` | `204` |

**`POST /api/pairing`**. Rate limit 60/min/IP. Code valid 15 min, poll every `pollIntervalSeconds`.

<details><summary>Response 200</summary>

```json
{
  "pairingId": "62300516-0013-464a-8b3b-f9d9014322cd",
  "code": "F2UVSP",
  "pollSecret": "wM5UNy\u2026<redacted>",
  "expiresAt": "2026-09-21T18:08:15.0399844Z",
  "pollIntervalSeconds": 3
}
```
</details>


**`GET /api/pairing/{id:guid}`**. Header `X-Poll-Secret` (required). The `deviceKey` is returned exactly once. Wrong secret → 404.

<details><summary>Responses 200 (pending, then paired)</summary>

```json
[
  {
    "status": "Pending",
    "deviceId": null,
    "deviceKey": null
  },
  {
    "status": "Paired",
    "deviceId": "8d62f10f-c8cd-4682-a4b6-4721cbc19b2d",
    "deviceKey": "eJ0Sbw\u2026<reda\u2026"
  }
]
```
</details>


**`GET /api/player/manifest`**. Send `If-None-Match` with the last ETag → 304 when unchanged. Also records `LastSyncAt`. Media URLs are signed for 7 days.

<details><summary>Response 200 (ETag "d92b2c0753871690")</summary>

```json
{
  "version": "d92b2c0753871690",
  "generatedAt": "2026-09-21T17:53:15.9084689Z",
  "device": {
    "id": "8d62f10f-c8cd-4682-a4b6-4721cbc19b2d",
    "name": "Lobby TV",
    "orientation": "Landscape",
    "timeZone": "Asia/Kolkata",
    "organizationName": "Demo Signage Co"
  },
  "defaultPlaylistId": "3af4ab00-1776-4872-8a27-ff38650a43d4",
  "schedules": [
    {
      "id": "00f6560b-5c82-4c45-8f7a-ec81ae90e92d",
      "name": "Breakfast menu",
      "priority": 20,
      "layoutId": null,
      "playlistId": "3af4ab00-1776-4872-8a27-ff38650a43d4",
      "startDate": "2026-09-21",
      "endDate": null,
      "startTime": "07:00",
      "endTime": "11:00",
      "daysOfWeek": 62
    }
  ],
  "layouts": [],
  "playlists": [
    {
      "id": "3af4ab00-1776-4872-8a27-ff38650a43d4",
      "name": "Browser loop 026742",
      "shuffle": false,
      "items": [
        {
          "mediaId": "365671ed-d308-49b6-bec5-a8579307b9b1",
          "durationSeconds": 10,
          "transition": "fade"
        }
      ]
    }
  ],
  "media": [
    {
      "id": "365671ed-d308-49b6-bec5-a8579307b9b1",
      "name": "brand-teal",
      "type": "Image",
      "mimeType": "image/png",
      "sha256": "06d1dd9a36abd525d2f99959ea0b645336565cfe9da096030646114ff938b62c",
      "sizeBytes": 4318,
      "durationSeconds": 10,
      "url": "/api/files/365671edd30849b6bec5a8579307b9b1?exp=1790618400&sig=<hmac>",
      "sourceUrl": null
    }
  ]
}
```
</details>


**`POST /api/player/heartbeat`**. Same payload as the SignalR `Heartbeat` method. Marks the device Online. 204.

**`POST /api/player/playback`**. Array of `{ mediaId, playlistId, playedAt (UTC), durationSeconds }`, max 1000 per call; entries older than 30 days or >5 min in the future are dropped. 204.

<details><summary><code>CreatePairingRequest</code> fields</summary>

| Field | Type |
|---|---|
| `deviceType` | string · nullable |
| `hardwareId` | string · nullable |
| `resolution` | string · nullable |
| `appVersion` | string · nullable |
| `osVersion` | string · nullable |
</details>

<details><summary><code>HeartbeatRequest</code> fields</summary>

| Field | Type |
|---|---|
| `appVersion` | string · nullable |
| `osVersion` | string · nullable |
| `resolution` | string · nullable |
| `syncedVersion` | string · nullable |
| `currentItem` | string · nullable |
| `freeStorageBytes` | integer (int64) · nullable |
</details>

## 5.13 Notifications and audit log

| Method | Path | Auth / permission | Request body | Response |
|---|---|---|---|---|
| `GET` | `/api/notifications` | any signed-in user | — | `200` |
| `POST` | `/api/notifications/{id:guid}/read` | any signed-in user | — | `204` |
| `POST` | `/api/notifications/read-all` | any signed-in user | — | `200` |
| `GET` | `/api/audit-logs` | `audit.view` | — | `SignageCms.Application.Common.PagedResult`1[[AuditLogDto, SignageCms.Application, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null]]` |

**`GET /api/notifications`**. Query `unreadOnly`, `take` (≤200). Returns `{ items, unreadCount }`.

**`GET /api/audit-logs`**. Query `page`, `pageSize` (≤200), `entityType`, `search` (summary, user or action).


## 5.14 Real-time hubs (SignalR)

Clients use `@microsoft/signalr` 8 with the JSON protocol. Pass the credential through `accessTokenFactory`.

**`/hubs/admin`** (JWT). Joins group `org:{orgId}` on connect.

| Event | Payload |
|---|---|
| `DeviceStatus` | `{ id, status: "Online"|"Offline", lastSeenAt, currentItem, syncedVersion }` |
| `Notification` | `{ id, createdAt, severity, category, title, message, link, isRead }` |

**`/hubs/device`** (device key). Joins `device:{deviceId}` and `org-devices:{orgId}`. Connecting marks the device Online; the last disconnect marks it Offline.

| Direction | Name | Payload |
|---|---|---|
| server → device | `ContentChanged` | none: call `GET /api/player/manifest` with `If-None-Match` |
| server → device | `Command` | `{ command, payload }`, command ∈ `identify`, `refresh`, `reload`, `clear-cache` |
| server → device | `Revoked` | none: discard the key and cached content, show pairing |
| device → server | `Heartbeat(status)` | `{ appVersion, osVersion, resolution, syncedVersion, currentItem, freeStorageBytes }` |

```js
const conn = new signalR.HubConnectionBuilder()
  .withUrl("/hubs/device", { accessTokenFactory: () => deviceKey })
  .withAutomaticReconnect().build();
conn.on("ContentChanged", () => sync());
conn.on("Command", ({ command }) => handle(command));
conn.on("Revoked", () => forgetDevice());
await conn.start();
setInterval(() => conn.invoke("Heartbeat", { appVersion: "1.0", syncedVersion }), 30000);
```

## 5.15 Operational endpoints

| Path | Purpose |
|---|---|
| `GET /health` | Liveness: `Healthy` (text). Doesn't check the database. |
| `GET /swagger` · `/swagger/v1/swagger.json` | OpenAPI 3 UI and spec (when enabled) |
| `GET /player` | Web player |
| `GET /downloads/signage-player.apk` | Android app (served from `wwwroot/downloads`) |
| `GET /*` | Admin SPA (`index.html` fallback) |

## 5.16 Versioning and compatibility

The API is unversioned (`v1` in Swagger). Players in the field depend on `/api/pairing`, `/api/player/*`, `/api/files/*` and `/hubs/device`. Treat those as a **public contract**: add fields freely, but never remove or rename them. If a breaking change is unavoidable, add new routes (e.g. `/api/v2/player/manifest`) and keep the old ones until every screen has updated.
